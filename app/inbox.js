import React from 'react';

var Inbox = React.createClass({
    render: function() {
        return (
            React.DOM.div(null, 
                React.DOM.h1(null, "Inbox!")
            )
        );
    }
});

export default Inbox;
